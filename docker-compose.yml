

services:
  web:
    build: .
    image: jotly:latest
    container_name: jotly
    restart: always
    ports:
      - "8000:8000"
    volumes:
      - ./db.sqlite3:/app/db.sqlite3
    env_file:
      - .env
